spring:

  application:
    name: catalog-service

  datasource:
    url: jdbc:postgresql://catalog-postgres:5432/catalog_db
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

server:
  port: 8081


management:
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/